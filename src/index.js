import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
//always remember that the components should be in block letters

//self clsoing tag ->given down

ReactDOM.render(
  <div>
    {/* <Heading></Heading> */}
    <App />
  </div>,
  document.getElementById("root")
);
